@startuml

start
:CommandList.getLastCommand();

if (CommandList.isEmpty()) then (yes)
  :throw exception;
  :console show error message;
else ([else])
  :return CommandResult to LogicManager;
  :console show successful message;
  :UI auto-fill the textbox
  with the most recent command;
endif

stop

@enduml
